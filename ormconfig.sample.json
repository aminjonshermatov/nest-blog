{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "dev",
  "password": "devpass",
  "database": "blog",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "dropSchema": false,
  "migrationsRun": false,
  "logging": true,
  "migrations": ["dist/src/**/db/migrations/**/*{.ts,.js}"],
  "cli": {
    "migrationsDir": "db/migrations"
  }
}
